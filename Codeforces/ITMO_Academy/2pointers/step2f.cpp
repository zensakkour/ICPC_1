#include<bits/stdc++.h>
using namespace std;
using lli = long long;

int main(){
    int n;
    lli k;
    cin >> n >> k;
    vector<lli> a(n);
    for(int i = 0;i < n;i++){
        cin >> a[i];
    }

    
}